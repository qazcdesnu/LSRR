# 확인 런 — F-031 수정이 실제로 작동하는지 짧게 본다.
#
# 보는 것은 정확도의 절대값이 아니라 **anytime 곡선이 평평한가**다. 수정 전에는
# m0~m7 이 전부 같은 값이었다 — 다중 토큰을 쓰도록 학습된 적이 없었기 때문이다.
# 여전히 평평하면 15런을 또 헛돈다.
defaults: [exp/prosqa_gpt2]

train:
  epochs: 2
  log_every: 200
